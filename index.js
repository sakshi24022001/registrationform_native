/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppTextInput from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppTextInput);
